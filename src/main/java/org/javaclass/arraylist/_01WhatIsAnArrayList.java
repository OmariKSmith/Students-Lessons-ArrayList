package org.javaclass.arraylist;

import java.util.ArrayList;
import java.util.List;

public class _01WhatIsAnArrayList {
//-                                             ArrayList

//`                 An ArrayList is a resizable array implementation of the List interface in Java.

//`                 Unlike regular arrays, which have a fixed size, an ArrayList can grow or shrink
//`                 as you add or remove elements.


                    List list = new ArrayList();

/*
                                    Key Characteristics
`                   Dynamic sizing: Automatically resizes as needed.

`                   Indexed access: You can access elements by index (list.get(0)).

`                   Ordered: Maintains the order in which elements were added.

`                   Allows duplicates: You can add the same element multiple times.
 */
}
